# Password Generator

A zero-dependency password generator built as a single HTML file.

## What I Learned

- Using crypto.getRandomValues() for cryptographically secure random generation in the browser instead of Math.random()
- Building a zero-dependency web app with no npm, no build step, and no framework as a single HTML file